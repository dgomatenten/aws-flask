Required GitHub repository secrets for .github/workflows/deploy-ecs.yml

AWS_ROLE_TO_ASSUME=arn:aws:iam::<account-id>:role/<github-oidc-deploy-role>
AWS_REGION=<region>
ECR_REPOSITORY=aws-flask-auth
ECS_CLUSTER=<ecs-cluster-name>
ECS_SERVICE=<ecs-service-name>

Notes:
- Keep DB credentials and app secret in AWS Secrets Manager, not in GitHub secrets.
- ECS task definition should reference Secrets Manager keys through containerDefinitions[].secrets.
